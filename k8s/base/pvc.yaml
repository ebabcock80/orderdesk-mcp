apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: orderdesk-data
  labels:
    app: orderdesk-mcp
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard  # Override in overlays for specific providers

